# Root .env.example
#
# Copy this to .env and fill in real values.
# NEVER commit a .env file with real secrets.
#
# Each app (apps/api, apps/web) has its own .env.example
# for app-specific variables.

# ── Shared / CI ──────────────────────────────────────
NODE_ENV=development
